name: flutter_weather_api
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  geocoding_resolver: ^0.0.3+2
  geolocator: ^10.1.0
  get: ^4.6.6
  http: ^0.13.6
  intl: ^0.18.1
  sleek_circular_slider: ^2.0.1

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
